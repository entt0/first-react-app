import s from './Dialogues.module.css';

const Dialogues = (props) => {
    return (
        <div>
            Here I want to create a page with messages
        </div>
    );
}

export default Dialogues;